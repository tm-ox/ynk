---
import { Icon } from "astro-icon";

const { name, cls } = Astro.props;
---

<Icon
  name={name}
  class="text-primary hover:text-hover transition rounded-full text-4xl z-50 h-8 w-8 cursor-pointer"
/>
