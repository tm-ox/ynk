---
import { Icon } from "astro-icon";

const { name, cls } = Astro.props;
---

<Icon
  name={name}
  class="text-primary hover:text-hover transition rounded-full text-4xl z-50 h-12 w-12 cursor-pointer"
/>
